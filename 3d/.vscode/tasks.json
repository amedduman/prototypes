// tasks.json
{
  "version": "2.0.0",
  "tasks": [
      {
          "label": "Run",
          "type": "shell",
          "command": "clear && make && ./game",
          //"command": "clear && clang main.c -I include/  -L lib/ -lraylib -framework CoreFoundation -framework IOKit -framework Cocoa && ./a.out"
      }
  ]
}
/*
note: below shortcut was added to keybindings.json to call this task with a shortcut
    {
      "key": "[IntlBackslash]",
      "command": "workbench.action.tasks.runTask",
      "args": "Run"
  },
*/
